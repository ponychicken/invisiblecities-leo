Irene.prototype.setupAnimation = function () {
  this.animTimeline = new Timeline();

  // var ele = this.newTween(this.get('elevator'), this.animTimeline);
  // ele.add(0, {y: 105}).add(8000, {y: -95}).add(16000, {y: 100});
  // ele.loop = true;
  //
  // var bubble = this.newTween(this.get('bubble'), this.animTimeline);
  // bubble.add(0, {y: 10}).add(3000, {y: -15}).add(6000, {y: 10});
  // bubble.loop = true;
  //
  // var hair = this.newTween(this.get('hair'), this.animTimeline);
  // hair.add(0, {x: 0}).add(400, {x: 3}).wait(10000).to({x:0}, 400);
  // hair.loop = true;
  //
  // var nose = this.newTween(this.get('nose'), this.animTimeline);
  // nose.add(0, {x: 0}).add(400, {x: -3}).wait(10000).to({x:0}, 400);
  // nose.loop = true;
};
